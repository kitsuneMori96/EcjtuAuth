# 更新日志

## v1.1.0 - 2026-08-27

首个正式稳定版。修复认证协议层关键 bug，移除不可靠功能，优化自动连接体验。

### 修复

- **eportal URL 端口编码错误**：`Uri.http(host, ':801/eportal/')` 将 `:801` 编码为路径 `%3A801`，服务器返回 `203 Bad request(1)`。改为 `Uri.http('host:801', '/eportal/')` 正确传递端口
- **假在线误判**：`probeInternet()` 仅检查 HTTP 2xx 状态码，captive portal 劫持后返回 `200+HTML` 被误判为在线。现对 `generate_204` URL 严格要求 204 状态码，普通 URL 校验非空 body 且不含 HTML 标签
- **IP 提取带尾部空格**：`extractIp()` 返回值未 trim，`wlanuserip` 带尾部空格可能导致服务器拒绝
- **认证后探测过快**：POST 登录后立刻探测外网，服务器尚未生效即被 captive portal 拦截。增加 800ms + 1s 两次探测窗口

### 移除

- **注销校园网功能**：eportal 注销接口返回 `203 Bad request(2)`，参数格式无文档可考；且该功能与「保持在线」定位无关
- 状态刷新后的白色 SnackBar 通知

### 新增

- **启动自动认证**：App 前台即自动尝试认证，无需手动点击
- **WiFi SSID 检测**：Android 运行时请求定位权限，修复 WiFi 名称显示「未检测到」
- **回前台刷新**：回到前台时同步刷新 WiFi 名称 + 自动连接
- **登录响应详情日志**：显示服务器状态码、请求 URL、账号字段、响应内容，便于定位

### 优化

- `connectOnce()` 已在线时直接返回，避免焦点变化时无意义重复检测
- `resumed` 生命周期仅调 `connectOnce()`，删除重复 `_refreshAll()`，消除并行双重 login POST 竞态

## v1.0.4-beta - 2026-08-26

### 移除

- **注销校园网功能**：eportal 注销接口返回 `203 Bad request(2)`，参数格式无文档可考，继续调试性价比低；且该功能与工具「保持在线」的定位无关。已从界面与协议层完整移除
- 移除随注销引入的「用户主动注销」标志与相关自动连接拦截逻辑

### 界面

- 移除状态刷新后的白色 SnackBar 通知（状态卡片本身已实时反映状态），消除暗色主题下的视觉突兀

## v1.0.3 - 2026-08-26

### 修复

- **注销"假成功"**：HTTP 2xx 仅代表服务器收到请求，不代表注销生效。现改为注销后等待并实测外网连通性
- **刷新状态 / 手动检测按钮无反应**：检测期间最长可达 12 秒却无任何反馈。现检测中状态卡显示转圈、按钮禁用

## v1.0.0~v1.0.2 - 2026-08-26

首个发布版本及迭代修复。基于 [Apauto-to-all/AutoAuthorize](https://github.com/Apauto-to-all/AutoAuthorize) 全面重写，单一 Flutter 代码库覆盖 Windows 与 Android。
